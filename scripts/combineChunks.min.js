import{existsSync,mkdirSync,writeFileSync}from"fs";import{join}from"path";const outputDir=join("dist","data");existsSync(outputDir)||mkdirSync(outputDir,{recursive:!0});const chunks=[process.env.EXCEL_FILE_CHUNK_1,process.env.EXCEL_FILE_CHUNK_2,process.env.EXCEL_FILE_CHUNK_3,process.env.EXCEL_FILE_CHUNK_4,process.env.EXCEL_FILE_CHUNK_5,process.env.EXCEL_FILE_CHUNK_6].filter(Boolean);if(6!==chunks.length)throw new Error("Missing one or more chunks");const binaryChunks=chunks.map((chunk=>Buffer.from(chunk,"base64"))),decoded=Buffer.concat(binaryChunks),outputFile=join(outputDir,process.env.VITE_EXCEL_FILE);writeFileSync(outputFile,decoded),console.log(JSON.stringify({size:decoded.length,path:outputFile}));